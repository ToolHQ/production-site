load_module modules/ngx_http_geoip2_module.so;
load_module modules/ngx_stream_geoip2_module.so;
load_module modules/ngx_http_headers_more_filter_module.so;

user nginx;
worker_processes 1;

error_log /var/log/nginx/error.log warn;
pid       /var/run/nginx.pid;

events {
  worker_connections 1024;
}

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  more_clear_headers 'Server';

  log_format main escape=json '{'
    '"app@timestamp": "$time_iso8601_p1.$millisec",'
    '"action": "Request received",'
    '"message": "$request $status $request_time $geoip2_city_country_name - $geoip2_region_code - $geoip2_city ($geoip2_latitude, $geoip2_longitude) $http_user_agent",'
    '"http": {'
      '"host": "$host",'
      '"remote_addr": "$remote_addr",'
      '"x_forwarded_for": "$http_x_forwarded_for",'
      '"request": {'
        '"request": "$request",'
        '"method": "$request_method",'
        '"uri": "$request_uri",'
        '"scheme": "$scheme",'
        '"referer": "$http_referer",'
        '"user_agent": "$http_user_agent",'
        '"upstream_response_time": "$upstream_response_time",'
        '"upstream_connect_time": "$upstream_connect_time"'
      '},'
      '"response": {'
        '"status": $httpStatus,'
        '"upstream_status": "$httpUpstreamStatus",'
        '"body_bytes_sent": $httpBodyBytesSent,'
        '"content_type": "$sent_http_content_type"'
      '},'
      '"elapsed_time": $elapsedTime,'
      '"proxy": {'
        '"host": "$proxy_host",'
        '"header_host": "$http_host",'
        '"uri": "$uri"'
      '},'
      '"location": {'
        '"lat": $locationLat,'
        '"lon": $locationLon'
      '},'
      '"address": {'
        '"geoipSource": "$geoipSource",'
        '"country": {'
          '"code": "$geoip2_city_country_code",'
          '"name": "$geoip2_city_country_name"'
        '},'
        '"city": {'
          '"id": "$geoip2_city_geoname_id",'
          '"name": "$geoip2_city"'
        '},'
        '"region": {'
          '"code": "$geoip2_region_code",'
          '"name": "$geoip2_region_name"'
        '}'
      '}'
    '},'
    '"req-id": "$request_id"'
  '}';

  access_log /var/log/nginx/access.log  main;
  proxy_busy_buffers_size               512k;
  proxy_buffers                         4 512k;
  proxy_buffer_size                     256k;

  sendfile                              on;
  # tcp_nopush                            on;

  keepalive_timeout                     65;
  gzip                                  on;

  map $http_x_forwarded_for $geoipSource {
    default "$http_x_forwarded_for";
    ""      "$SOURCE_IP";
  }
  # GeoIP2
  geoip2 /usr/share/geoip/GeoLite2-Country.mmdb {
    auto_reload 5m;
    $geoip2_metadata_country_build metadata build_epoch;
    $geoip2_country_code source=$geoipSource country iso_code;
    $geoip2_country_name source=$geoipSource country names en;
    $geoip2_country_geoname_id source=$geoipSource country geoname_id;
    $geoip2_continent_code source=$geoipSource continent code;
    $geoip2_continent_name source=$geoipSource continent names en;
    $geoip2_continent_geoname_id source=$geoipSource continent geoname_id;
  }
  geoip2 /usr/share/geoip/GeoLite2-City.mmdb {
    auto_reload 5m;
    $geoip2_metadata_city_build metadata build_epoch;
    $geoip2_city_country_code source=$geoipSource country iso_code;
    $geoip2_city_country_name source=$geoipSource country names en;
    $geoip2_city_country_geoname_id source=$geoipSource country geoname_id;
    $geoip2_city source=$geoipSource city names en;
    $geoip2_city_geoname_id source=$geoipSource city geoname_id;
    $geoip2_postal_code source=$geoipSource postal code;
    $geoip2_dma_code source=$geoipSource location metro_code;
    $geoip2_latitude source=$geoipSource location latitude;
    $geoip2_longitude source=$geoipSource location longitude;
    $geoip2_time_zone source=$geoipSource location time_zone;
    $geoip2_region_code source=$geoipSource subdivisions 0 iso_code;
    $geoip2_region_name source=$geoipSource subdivisions 0 names en;
    $geoip2_region_geoname_id source=$geoipSource subdivisions 0 geoname_id;
    $geoip2_subregion_code source=$geoipSource subdivisions 1 iso_code;
    $geoip2_subregion_name source=$geoipSource subdivisions 1 names en;
    $geoip2_subregion_geoname_id source=$geoipSource subdivisions 1 geoname_id;
    $geoip2_city_continent_code source=$geoipSource continent code;
    $geoip2_city_continent_name source=$geoipSource continent names en;
  }
  geoip2 /usr/share/geoip/GeoLite2-ASN.mmdb {
    auto_reload 5m;
    $geoip2_metadata_asn_build metadata build_epoch;
    $geoip2_asn source=$geoipSource autonomous_system_number;
    $geoip2_org source=$geoipSource autonomous_system_organization;
  }
  map "$time_local:$msec" $time_local_ms { ~(^\S+)(\s+\S+):\d+\.(\d+)$ $1.$3$2; }
  map $time_iso8601 $time_iso8601_p1 {
    ~([^+]+) $1;
  }
  map $msec $millisec {
    ~\.([0-9]+)$ $1;
  }
  map $geoip2_latitude $locationLat {
    default "$geoip2_latitude";
    ""      "null";
  }
  map $geoip2_longitude $locationLon {
    default "$geoip2_longitude";
    ""      "null";
  }
  map $status $httpStatus {
    default "$status";
    ""      "null";
  }
  map $upstream_status $httpUpstreamStatus {
    default "$upstream_status";
    ""      "null";
  }
  map $request_time $elapsedTime {
    default "$request_time";
    ""      "null";
  }
  map $body_bytes_sent $httpBodyBytesSent {
    default "$body_bytes_sent";
    ""      "null";
  }

  include /etc/nginx/conf.d/*.conf;
}