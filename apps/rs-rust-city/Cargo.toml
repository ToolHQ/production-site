[package]
name = "rs_rust_city"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.16.0"
bevy_rapier2d = "0.30.0"
bevy_egui = "0.34.1"
rand = "0.9.1"

# sudo apt update
# sudo apt install libudev-dev libasound2-dev pkg-config
