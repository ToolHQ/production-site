# Build artifacts
target/
**/target/

# Local secrets and dev files
.env
.env.*
!.env.example

# Local SQLx prepared queries cache (regenerated in CI)
.sqlx/

# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# Git metadata (build does not need git history)
.git/
.gitignore

# Docs and tasks (not needed at build time)
README.md
docker-compose.yaml
justfile
