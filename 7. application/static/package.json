{
  "name": "static",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "ts-build": "tsc && cp -R ./assets/* ./dist",
    "upload": "aws --endpoint-url http://minio.localhost s3 sync --delete ./dist s3://my-site/static/",
    "ts-build-and-upload": "npm run ts-build && npm run upload",
    "build-watch": "nodemon --ignore dist --ext ts,css,html,ico --exec npm run build-and-upload",
    "build": "webpack",
    "build-and-upload": "webpack && cp -R src/ dist/src/ && cp -R assets/* dist/ && npm run upload"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "3.1.0",
    "ts-loader": "9.5.1",
    "typescript": "5.4.5",
    "webpack": "5.91.0",
    "webpack-cli": "5.1.4",
    "webpack-dev-server": "5.0.4"
  }
}
