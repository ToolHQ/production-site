{
  "name": "static",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "build": "tsc && cp -R ./assets/* ./dist",
    "upload": "aws --endpoint-url http://minio.localhost s3 sync ./dist s3://my-site/static/"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
