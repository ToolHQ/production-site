# CI migrado para Jenkins SSDNodes — stage changelog em pipeline.yaml (branch main)
#
# git-cliff via tools/citools/scripts/changelog-update.sh

name: Auto Documentation (retired → Jenkins)

on:
  workflow_dispatch:

jobs:
  migrated:
    name: Auto-docs no Jenkins SSDNodes
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "CHANGELOG auto-update migrado para Jenkins (main only)."
          echo "https://jenkins.ssdnodes.dnor.io/job/production-site/"
          exit 0
