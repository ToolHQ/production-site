# Aposentado (T-365): CodeQL no Jenkins (pipeline.yaml stage codeql)
# Scan semanal = cron Jenkins (Fase 4 T-365), não GHA.

name: CodeQL Security Scan (retired — use Jenkins)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::CodeQL via GHA aposentado. Jenkins production-site — stage CodeQL."
          exit 1
