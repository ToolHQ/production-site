# Aposentado (T-365): proxy release via Jenkins + T-364 installers
# Emergência manual apenas.

name: Release agent-meter-proxy (retired — use Jenkins)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag (agent-meter-proxy-vX.Y.Z)'
        required: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Release proxy via GHA aposentado. Jenkins + T-364."
          exit 1
