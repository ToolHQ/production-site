# Aposentado (T-365): validação OTLP no Jenkins (pipeline.yaml → agent-meter-validate)
# Emergência manual apenas.

name: Agent Meter Validation (retired — use Jenkins)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Use Jenkins production-site multibranch — stage Agent Meter OTLP."
          echo "https://jenkins.ssdnodes.dnor.io/job/production-site/"
          exit 1
