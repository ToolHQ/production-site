# Aposentado (T-365): release via Jenkins SSDNodes (Fase 3)
# Emergência: workflow_dispatch com tag manual no Jenkins.

name: Release agent-meter (retired — use Jenkins)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag (agent-meter-vX.Y.Z)'
        required: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::Release GHA aposentado — macos-latest custava $$. Use Jenkins (T-365)."
          exit 1
