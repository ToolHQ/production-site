# CI migrado para Jenkins SSDNodes — stage CodeQL em pipeline.yaml
#
# Análise JS/TS + Python via tools/citools/scripts/codeql-scan.sh

name: CodeQL (retired → Jenkins)

on:
  workflow_dispatch:

jobs:
  migrated:
    name: CodeQL no Jenkins SSDNodes
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "CodeQL migrado para Jenkins stage codeql (citools)."
          echo "https://jenkins.ssdnodes.dnor.io/job/production-site/"
          exit 0
