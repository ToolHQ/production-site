# CI migrado para Jenkins SSDNodes — https://jenkins.ssdnodes.dnor.io/job/production-site/
#
# Quality gates (shell, rust, bats, js, yaml) rodam via citools + tools/harness/verify.sh
# no stage verify-branch. Re-disparo manual apenas para debug legado.

name: Quality Gates (retired → Jenkins)

on:
  workflow_dispatch:

jobs:
  migrated:
    name: CI no Jenkins SSDNodes
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Quality Gates migrados para Jenkins + citools."
          echo "https://jenkins.ssdnodes.dnor.io/job/production-site/"
          exit 0
