# Copy to /opt/k8s-ops/watchdog.env on the master (install_health_watchdog.sh).
# Mode 640 root:root — readable by k8s-health-check.service (User=root).
#
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
