# Componentes arquivados

Pastas com prefixo `_` **não aparecem** no menu de deploy da TUI (`deploy_components.sh`).

| Componente | Motivo |
|------------|--------|
| `elastic-stack` | Pesado para OCI; substituir por Vector → Parquet no MinIO |
