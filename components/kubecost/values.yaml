cost-analyzer:
  resources:
    requests:
      cpu: 100m
      memory: 400Mi
    limits:
      cpu: 300m
      memory: 600Mi
  
  frontend:
    resources:
      requests:
        cpu: 20m
        memory: 32Mi
      limits:
        cpu: 100m
        memory: 128Mi

prometheus:
  server:
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 300m
        memory: 512Mi
    retention: 5d # Reduce retention to save disk space if configured
