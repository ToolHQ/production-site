spec:
  template:
    spec:
      containers:
        - name: longhorn-ui
          resources:
            requests:
              cpu: 10m # was 50m, then 15-25m — UI only, not data path (T-103)
              memory: 64Mi
            limits:
              cpu: 150m
              memory: 128Mi
